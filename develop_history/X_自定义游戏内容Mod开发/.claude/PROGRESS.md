# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-16（会话 02）

---

## 现在在哪

**X1「第 4 个基础角色爱琳」—— 代码侧全部做完，实机已能选到、进图、开枪；本轮重做了 1 号 / 3 号武器和持枪动作，⏳ 等实机验收。**

结论已落定，**不用重新调研**：图里那个角色 = `아이린` / 爱琳 / `ChrIndex=3`，
311 客户端里她**只差模型**（§1）；挡着她的是**六道 exe 闸 + 一道数据闸**（§2）；
中文版大头像图集只声明 24 帧那道坑已补（§3）。

| 里程碑 | 状态 |
|---|---|
| M0 本套进度文件 + 根目录指针 | ✅ |
| M1 `tools/mkchar.py` 克隆 ch01 → ch03（672 文件 / 20 MB） | ✅ |
| M2 图集 24→28 帧 + 删 `OpenLocale` + 退役 `REGION_SITES[2]` | ✅ |
| M3 廉价证伪 | ➖ 被 M1 离线体检 + §10 取代 |
| M4 hook 九处字节补丁（6 解锁 + 3 级门，级门默认不打） | ✅ 实机确认 |
| M5 服务端三处常量 + 测试 | ✅ 两套运行时全绿 |
| M6 实机验证 | ✅ 角色显示正常、能进图、能打（2026-09-15 用户） |
| M7 外观改造：头发 / 身体 / 裙摆 / 武器 | ✅ 代码侧；⏳ **本轮武器 + 动作等实机** |

**本轮（会话 02）做了什么**：用户看了 `compare_E_*.png` 后指出 1 号武器该是**一把双手大步枪**、
3 号该是**长柄花杖**，持枪动作要像瓦尔基里。已做：

- `tools/mtntool.py` 补 `.mtn` 写出器（1343 个文件逐字节 round-trip）。
- `tools/ch03_skin/anim.py`：Stand01 / Attack01 / Reload01 / Run-F01 / Run-B01 / Idle101 六个动作
  从 ch102 瓦尔基里**移植上半身旋转**（§20、D9）；两把武器的枪口点静态骨补丁打进全部 86 个 `.mtn`（§19、D10）。
- `tools/ch03_skin/rig.py`：步枪 / 手杖的摆放框架 + 枪口点位置，anim.py 和 weapons.py 共用。
- `tools/ch03_skin/weapons.py`：`ch03W0001` 一把 45.7 长的步枪（只绑 `Bone_Wp01_R02`）、`ch03W0003` 花杖，贴图重画。
  W0002 / D0002 / D0003A 一字节没动。
- **第二轮修改（用户看图后）**：步枪上下叶翼改成尖朝枪口（原先朝枪把，反了）；花头改成 **4 瓣半开花苞 + 硬棱水晶感**、
  圆形黄花蕊 + 一根前凸长尖（尖端 = 枪口，`rig.STAFF.muzzle_a=50`）；两片黄萼片立起来、略向花倾、宽面朝侧。
  ★ 对照图最下两行是**弹体**（D0002 种球、D0003A 回血药水图腾），不是手持件的另一角度，行标签已写明；
  卡希尔那格是模板文件，她实机的 3 号弹体是 2D 精灵火箭弹（§16）。
- `mkchar.py --audit` 第 ④ 条放宽（骨架树一致即可），七条全过；`build-pack` 已重打 ch03 卷；客户端已重起（登录界面存活，无崩溃报告）。

对照图：`tools/ch03_skin/compare_E_weapons.png`（单件）、`compare_E_inhand.png`（StandN / AttackN / Reload01 手持）、
`compare_F_anim.png`（移植后动作 9 帧）。

★★ **重建链的顺序**（全部幂等，母本都是只读的 ch01 / ch102）：
`mkchar.py → recolor.py → ears.py → geometry.py（最后改贴图尺寸）→ anim.py → weapons.py`。
`mkchar.py` 会**拒绝覆盖**被改过的文件（`--force` 才覆盖）。**weapons.py 必须在 anim.py 之后跑**
（它的手持对照图读 ch03 里移植后的动作；网格本身不依赖）。改武器位置 / 枪口点只改 `rig.py`，然后 anim.py + weapons.py 各跑一遍。

---

## 下一步

1. **等用户走一遍下面的 V7 ~ V9**（本轮新增）。
2. 实机不满意的地方按反馈调 `rig.py` / `weapons.py` 的常量；要是 Jump / Crouch / Dash 单手拎枪太难看，
   再把那几个动作也加进 `anim.CLIPS`。

---

## 当前卡点

无。**服务端（昨晚起的，PID 见端口 27799）和客户端都在跑**，可以直接登录测。
★ 客户端是我带 `BSHOOK_IRENE_LEVEL_FREE=1` 用 `tools\launch.ps1` 起的（昨晚那次也是，日志里 9 处补丁）——
`start.bat` 自己**不会**设这个变量；测试账号不到 4 级又用 start.bat 起的话，爱琳会弹「4 级开始使用」（D2）。
⚠ 上一轮遗留：`tools/build-ver.config` 从 `0.3.3` 变成了 `0.3.5`，来源不明，没动它，等用户确认是不是自己改的。

---

## ⏳ 待用户验证

**已实机确认正常的**（2026-09-15）：第 4 个角色出现且名字是爱琳、房间面板有她、能进图能打、
三把武器的弹道 / 爆炸 / 回血特效（原版真货，别动）、开枪有声（`ch03_sounds.py`）。
裙子网格线的修法（§17）代码侧已修，⏳ 未落锤。

**本轮新增**（用爱琳进一局，三把武器各用一遍）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V7 | 选 1 号武器站着不动、开几枪、打空 6 发等换弹、按住方向键跑 | **一把**绿身橙管的步枪，双手端在胸前偏左（像瓦尔基里）；开枪有轻微后坐；换弹时枪放低、右手往后拉；跑动时仍双手持枪。**枪口火花 / 弹道线从枪口方块前脸冒出**，不是从空气里 | ⏳ |
| V8 | 选 3 号武器站着、发射一次图腾 | 右手握一根长柄花杖平指前方（蓝花瓣 + 黄花蕊朝前，下绿上黄的杖身）；发射时抬杖，药水从花蕊尖飞出 | ⏳ |
| V9 | 拿着 1 号武器跳、蹲、冲刺、挨打、切到 2 号再切回 1 号 | 全程不闪退；这些动作里步枪跟着右手走、只是单手拎着（预期的，D9 的代价） | ⏳ |

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。
截图最好带侧面（能看出枪身长度和枪口位置）。

---

## 给接手会话的六句话

1. **图里那个角色叫爱琳，`ChrIndex=3`，原版埋好没开，只差 `Models/Characters/ch03/`。**
   别再重新调研，结论和证据全在 `FINDINGS.md` §1。
2. **M2 必须排在 M4 之前。** 不补 `BigChrIcons_CN.smf`（24→28 帧）就打开闸，
   **一进大厅就崩** `C0000005 @0x430857`（§3）。守卫在 `mkchar.py --audit` 第 ⑦ 条。
3. **六道闸，不是五道。** 最容易漏的是 `0x44caaf`（大厅面板）——
   大厅 / 房间 6 格 / 战斗换人条是**三条独立代码路径**（§2）。
4. **`bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 已经钉成字面量 `(0,1,2)`**，
   故意不跟 `BASE_CHARACTER_IDS`。别「顺手」改回派生写法（§8 / `test_bot` 有守卫）。
5. **`character_id_of_item(4400001)` 必须继续返回 `None`** —— 爱琳不是角色卡，
   给她补一张卡会把 `0x55853c` 那处补丁架空（`test_gameserver` 有守卫）。
6. **爱琳的 86 个 `.mtn` 不再是 ch01 的逐字节克隆**（anim.py 改写过：6 个动作移植 + 全部打了枪口点补丁）。
   改动作走 `anim.py`，别手拷别的角色的 `.mtn`（骨架树不同，查不到骨就崩，§20）。
