# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-15（会话 01）

---

## 现在在哪

**X1「第 4 个基础角色爱琳」—— 代码侧全部做完，⏳ 只剩实机验证。**

结论已落定，**不用重新调研**：图里那个角色 = `아이린` / 爱琳 / `ChrIndex=3`，
311 客户端里她**只差模型**（§1）；挡着她的是**六道 exe 闸 + 一道数据闸**（§2）；
还有一道必崩的坑是中文版大头像图集只声明 24 帧（§3）。

| 里程碑 | 状态 |
|---|---|
| M0 本套进度文件 + 根目录指针 | ✅ |
| M1 `tools/mkchar.py` 克隆 ch01 → ch03（672 文件 / 20 MB） | ✅ 体检 7 条全过、幂等 |
| M2 图集 24→28 帧 + 删 `OpenLocale` + 退役 `REGION_SITES[2]` | ✅ |
| M3 廉价证伪 | ➖ 已被 M1 的离线体检 + §10 取代，见下 |
| M4 hook 六处字节补丁 | ✅ 实跑确认 6 行全打上、0 报错 |
| M5 服务端三处常量 + 测试 | ✅ 两套运行时 3614 项全绿 |
| M6 实机验证 | ⏳ **等用户** |
| M7 外观改造（Fable 5.1 子代理） | ✅ **阶段 A+B+C 全部完成**，⏳ 等实机 |

**M7 做到哪了**：银白发（母本本来就是白的，§12）、浅灰绿眼、精灵尖耳、品红花瓣披风/袖、
淡粉花瓣裙摆、淡粉靴 + 黄绿叶片、头顶品红角状发簇 + 触角、**及膝辫子**。
新几何**按部位挂到原版自带的物理骨链上**（§13）：辫子绑满 `Hairtail_*01..07`、
裙摆绑满八向 `Skirt_*`、披风借 `Wing_*` / `Spine2` ⇒ 跑跳时会自己甩、自己飘。
头发件 150→432 顶点、9→23 骨；身体件 165→587 顶点、31→41 骨（原版先例 919 顶点/59 骨）。
贴图升到 128×128（旧图放左上象限、UV ×0.5）。
**改动只有 4 个文件**：`ch0300000.{msh,dds}` / `ch03B0000.{msh,dds}`。
对照图 `tools/ch03_skin/compare_C{,_posed,_bind}.png`。

**已核验**：257/257 `.msh` round-trip 逐字节一致；两个改过的网格引用的
**23 / 41 根骨全部在骨架里真实存在**（用 `mshtool`+`mtntool` 结构化解析查的）；
`mkchar.py --audit` 7 条全过；重建链 `mkchar → recolor → ears → geometry` 幂等。

**离线看得出来的两处偏差**（等用户定夺要不要回炉）：
头顶那两根触角**偏长偏直**；及膝辫子在跳跃动作里**张开过大、偏硬**，像两根尖刺。

**原画里仍没做的**：皮肤提亮、腿部粉渐变、三把武器没改。**身形仍是卡希尔的。**

★★ **别顺手重跑 `mkchar.py`**：它现在会**拒绝覆盖**被改过的文件并把名单打出来
（`--force` 才覆盖）。真要从母本重建，之后必须**按顺序**跑
`recolor.py` → `ears.py` → **`geometry.py`（必须最后**，它会把贴图升成 128×128 并把 UV ×0.5）。

**M3 为什么取消**：它的目的是「先证明模型链路，再动 exe」。但 `mkchar.py --audit`
已经把同一件事**离线**证完了（每个 `.msh` 的贴图名都命中、骨架同源、
`weapon.ini` 点名的模型/特效 0 缺失），而「穿着别人的衣服切过来」那条风险
被 §10 证明是原版三个基础角色之间**本来就有**的不对齐。
⇒ 花用户一次实机操作去跑一个已经离线证完的结论不划算，直接并进 M6。

**已实机确认的（agent 自己跑的）**：客户端起得来、六处补丁全打上、
`地区差异` 从 5 条变 4 条、存活 270 s 无崩溃报告、`server.err` 空。

---

## 下一步

1. **等用户走一遍下面的「⏳ 待用户验证」**（V0 ~ V6）。
2. 用户确认 V1/V2 之后，把 M7 交给 Fable 5.1 子代理（D3）。

---

## 当前卡点

无。**服务端和客户端都在跑着**（`tools/launch.ps1` 起的），可以直接登录测。

---

## ⏳ 待用户验证

**★ 2026-09-15 第一次实机反馈（用户，Lv.1 账号）**：

1. **弹出「您可以从4级开始使用爱琳」** —— 这是**补丁全部生效的证据**，
   不是 bug：原版自己的 `0x467eb1` 房间面板拒绝逻辑，说明她的按钮已经建出来、也点得中。
   ⇒ 现在已用 `BSHOOK_IRENE_LEVEL_FREE=1` 解除，9 处补丁（6 解锁 + 3 级门）全打上、0 报错。
2. **她的图标是空白的** —— ✅ **已修**：`Images/Chinese/ChrIcons_CN.smf` 28 → 32 帧。
   ★ **小图集的帧序和大图集不一样**：爱琳在大图集是 `0x1a/0x1b`、
   在**小图集是 `0x1c/0x1d`**，中文版原来只声明 28 帧（0..0x1b）⇒ 越界画不出来。
   （教训：当初我只核了「帧数 ≥ 0x1b+1」就下结论「小图集不用改」，
   **没核帧序** —— 两个图集的排布根本不是一回事。）

**★ 2026-09-15 第二次实机反馈：角色显示正常、能进图、能打。** 剩两个外观问题：

| 现象 | 状态 |
|---|---|
| 裙子上有网格线 | 🟡 已交 Fable。**排除了**贴图（渐变干净）和 DDS 头（和母本约定一致：`flags=0x1007` 不含 `DDSD_PITCH`、`mips=0`、数据正好一层）⇒ 是 UV / 几何侧 |
| 裙子上有网格线 | ✅ 代码侧已修，⏳ **实机未落锤**。根因**不是 UV**（我判断错了）：是邻接表把双面花瓣的正面和反面配成了邻居，每条内部边都变成模板阴影的轮廓边（§17）。新配对规则**独立验证过**：ch00/ch01/ch02 复现原版导出结果 254/284、235/256、234/253（旧规则 0） |
| 手持武器 + 2/3 号**弹体**是卡希尔的 | ✅ 5 个 3D 网格全部重做（`W0001/0002/0003` + `D0002` + `D0003A`），照 `Ch003Weapons.png` 三帧。对照图 `tools/ch03_skin/compare_E_weapons.png` / `compare_E_inhand.png`。根因见 §16 |
| 开枪没声 | ✅ **已修**：`tools/ch03_sounds.py` 补 11 个音源。根因是 `weapon.ini` 的两种路径写法（§4） |

**以上全部已重打包 + 客户端已重启**（9 处补丁全打上、0 报错、无崩溃报告），⏳ 等实机验收。

⚠ **`tools/build-ver.config` 从 `0.3.3` 变成了 `0.3.5`，来源不明** ——
没有任何脚本会写它（`build-common.ps1:217` 注明「发版前手动改的」），
Fable 的脚本也没引用它。**没有动它**，等用户确认是不是他自己改的。

★ 实机确认**正常**的：三把武器的弹道特效、爆炸特效、3 号的回血效果 —— 都是
`Effects/CH03/` 和 `default.amf` 里的**原版真货**，别动。

⚠ 顺手修了 `tools/probe_char_list.py`：它是**用 Python 复刻判定链**、按脚本里的常量算的，
补丁打上后还在报「合计 3 个」骗人。常量已同步成 `(0,1,2,3)`，输出也注明了「不是从内存读的」。

**前置**：把原画另存到 `.claude/refs/irene-concept.png` ✅ 用户已放（实为 JPEG，Pillow 能开）。

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V0 | 用一个**拥有角色 110** 的号进关卡，看战斗内换人条 | 110 的头像从「希莉亚的脸」变成**它自己那张红发脸**。**和爱琳无关**，单验图集那一刀 | ⏳ |
| V1 | 登录进大厅，打开左边「人物选择」 | 基础角色从 **3 个变 4 个**；第 4 个是**白发尖耳品红**的少女；名字显示**爱琳**（不是 `???`） | ⏳ |
| V2 | 点她 → 建房 → 看房间右下的 6 格「人物选择」 | 大厅点得动（验 `0x44caaf`）；**房间面板里也有她**（验 `0x407383`，两条独立代码路径）；3D 预览是女性体型（**这一版长得像卡希尔，是预期的**） | ⏳ |
| V3 | 房间里来回切 爱琳 ↔ 泰尔 ↔ 爱琳，退房重进、重登 | 每次都换得动；重登之后**仍停在爱琳**（服务端存盘） | ⏳ |
| V4 | 用爱琳开一局，战斗中按换人键打开换人条 | 进关卡**不崩**；换人条上有爱琳的大头像 | ⏳ |
| V5 | 三把武器各开几枪（트릭스터 / 씨드 폭탄 / 에이리얼 슈터）+ 普攻 + 双击方向键冲刺 + 格斗模式连招 | 弹体和特效出得来（花瓣、蝴蝶、泡泡）；**开枪没声是预期的**（11 个武器 ogg 没补，§4） | ⏳ |
| V6 | 死一次、复活一次、打到结算；再加一个 bot 同房 | 全程不闪退；**bot 不能是爱琳**（`/c N M` 的 M 仍只有 1~3） | ⏳ |

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的六句话

1. **图里那个角色叫爱琳，`ChrIndex=3`，原版埋好没开，只差 `Models/Characters/ch03/`。**
   别再重新调研，结论和证据全在 `FINDINGS.md` §1。
2. **M2 必须排在 M4 之前。** 不补 `BigChrIcons_CN.smf`（24→28 帧）就打开闸，
   **一进大厅就崩** `C0000005 @0x430857`（§3）。守卫在 `mkchar.py --audit` 第 ⑦ 条。
3. **六道闸，不是五道。** 最容易漏的是 `0x44caaf`（大厅面板）——
   大厅 / 房间 6 格 / 战斗换人条是**三条独立代码路径**（§2）。
4. **`bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 已经钉成字面量 `(0,1,2)`**，
   故意不跟 `BASE_CHARACTER_IDS`。别「顺手」改回派生写法（§8 / `test_bot` 有守卫）。
5. **`character_id_of_item(4400001)` 必须继续返回 `None`** —— 爱琳不是角色卡，
   给她补一张卡会把 `0x55853c` 那处补丁架空（`test_gameserver` 有守卫）。
6. **外观那一块交给 Fable 5.1 子代理**（D3），主会话别顺手改。
   它的准入门槛是 `.msh` round-trip 逐字节一致，过不了就只改贴图。
