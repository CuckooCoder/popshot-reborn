# X_Mod PROGRESS —— 现在在哪

**只保留当前状态，全文 ≤ 150 行。** 做完的事从「正在做」挪走，不留历史；流水账进 `sessions/`。

最后更新：2026-09-16（会话 03）

---

## 现在在哪

**X1「第 4 个基础角色爱琳」—— 能选到、能进图、能打，三把武器的模型用户已验收正常。
本轮拆掉了她身上不该有的东西：卡希尔的锤子、卡希尔的抱熊冲刺。⏳ 等实机验收。**

结论已落定，**不用重新调研**：图里那个角色 = `아이린` / 爱琳 / `ChrIndex=3`，
311 客户端里她**只差模型**（§1）；挡着她的是**六道 exe 闸 + 一道数据闸**（§2）；
中文版大头像图集只声明 24 帧那道坑已补（§3）。

| 里程碑 | 状态 |
|---|---|
| M0 ~ M6 本套进度文件 / 克隆 / 图集 / 九处字节补丁 / 服务端常量 / 首次实机 | ✅ |
| M7 外观改造：头发 / 身体 / 裙摆 / 三把武器 / 持枪动作 | ✅ **用户已验收**（2026-09-16：进游戏正常、武器模型正常）|
| M8 去卡希尔化：删锤子 + 冲刺攻击改瓦尔基里 | ⏳ **本轮做完，等实机** |

**本轮（会话 03）做了什么** —— 用户实机反馈五条，都做完了。
①② 第一批 → 验收后给了 ③④ → 再验收后给了 ⑤：

5. **3 号武器发射音不再是卡希尔的**。用户问「艾琳没有自带的音效吗」——
   对着原版解密树逐目录数过：**没有**，她全客户端只有 `CharacterChanger/ch003.ogg`
   一个原生音（选人时念名字），武器音 11 个**全是借的**（§24）。
   3 号原先借的是卡希尔 `ch01@Attack03.ogg`（캐논왈츠 火箭炮，1.53 s 轰鸣），换成
   `FX/wp/common/weapon@watergun.ogg` —— **原版发了却没人引用的音**（`FX/wp/common/` 整个目录只有它，
   ini / evn / efx / exe 里都搜不到）。0.51 s、低频只占 11%，对她那把抛药水的正好。
   改的是 `tools/ch03_sounds.py` 的 `MAPPING` 两行。

第一批 ①②、第二批 ③④：

3. **剩下的格斗 / 表情特效也改成她自己的**。`anim.EVN_EFX_REMAP` 扩成一张**全局路径映射表**
   （16 条，对全部 61 个 `.evn` 生效，D13）：母本引用的 24 个特效逐个去 `Effects/` 找 CH03 版，
   **有就换、没有就留**。`Jab00` / `DashAttack01..05` / `Dash04` / `Dash-B04` 原版没给爱琳画，继续用卡希尔的。
4. **空手跑的摆臂**（`ch03@Run-F00`）。根因：`Run-F%02d` 的 N = 武器的 `WAnimIdx`，
   空手 / 格斗模式跑放 `Run-F00`，而卡希尔那套是**单手托着熊跑**（右前臂几乎不动）。
   源取 **ch00 泰尔**（和爱琳同为 `DisplayHeight=75`、前臂摆幅左右恰好相等），
   **不是**瓦尔基里（她把步枪架在左臂上跑，是全表最不对称的一个，§23）。
   ★ 光抄旋转不够 —— 两套循环起点差 1/8 周期，`anim.leg_sync_phase()` 把一个周期试一遍自己算出偏移
   （−3 帧），「同侧手·同侧脚」从 **−0.31 → −0.97**，左右手前后行程 20.3/16.2 → 41.2/41.8（D14）。

第一批那两条：

1. **删掉卡希尔的锤子**（`ch03J0000`）。它是 `J` 类部件、绑 `Bone_Wp05_01`、**站姿里就拎在右手上**；
   16 个角色目录里**只有 ch01 有 `J` 件** ⇒ 删掉是回到另外 14 个角色的常态（§21）。
   做法：`tools/mkchar.py` 新增 `SKIP` 名单（体检 ① 反过来盯着它别再长回来），删 `.msh` + `.dds`。
2. **冲刺攻击改成瓦尔基里的**（`ch03@Dash00`）。根因：`[ch01-dash]` 没写 `WMeshIdx` ⇒ 默认取
   `ch01W0000` = **泰迪熊**，卡希尔那一下是「抱着熊撞过去」；`[ch03-dash]` 写的是 `WMeshIdx=-1`（空手）
   ⇒ 爱琳照搬就成了「抱空气」（§22）。
   做法：`tools/ch03_skin/anim.py` 的 `CLIPS` 每条动作自带「抄哪些骨」的选择器，
   `Dash00` 用 `take_biped`（全部 45 根 `Bip01_*`，不只上半身）、时长取爱琳自己的 1.0 s（D12）。
   顺手把 `ch03@Dash00.evn` 的两条特效从 `CH01/WP00/`（挂在**熊骨** `Bone_Wp00_11` 上）
   改指 `Effects/CH03/WP00/` 里**原版给她画好的**花瓣特效（挂 `Bip01_R_Hand`）。

对照图：`tools/ch03_skin/compare_G_dash.png`（冲刺，三行：卡希尔抱熊 / 瓦尔基里源 / 爱琳移植后）、
`compare_H_run.png`（空手跑，三行：卡希尔托熊跑 / ch00 源 / 爱琳移植后）。
`mkchar.py --audit` 七条全过（死引用仍 14 条）；`build-pack` 已重打 ch03 卷（671 文件）；
`update-gamedata` 跟着跑了，240 个测试全绿；`build-pack -Check` 卷和服务端数据都对得上。

★★ **重建链的顺序**（全部幂等，母本都是只读的 ch01 / ch102）：
`mkchar.py → recolor.py → ears.py → geometry.py（最后改贴图尺寸）→ anim.py → weapons.py`。
`mkchar.py` 会**拒绝覆盖**被改过的文件（`--force` 才覆盖）。**weapons.py 必须在 anim.py 之后跑**
（它的手持对照图读 ch03 里移植后的动作；网格本身不依赖）。改武器位置 / 枪口点只改 `rig.py`，然后 anim.py + weapons.py 各跑一遍。
⚠ `mkchar.py` 不加 `--no-prune` 会顺手删掉 `ch03D0003A.dds`（weapons.py 产出的，它不在克隆计划里）——
会打印「删除 1」，不是静默的；重跑 weapons.py 就回来了。

---

## 下一步

1. **等用户走一遍下面的 V10 / V12 / V13。**
2. 还没做、想到了记在这儿的：
   - **1 / 2 号武器的发射音现在借的是泰尔（ch00）的**，和 3 号原先的毛病同源（§24）。
     用户没提，先不动；哪天听着像他了，改 `ch03_sounds.py` 的 `MAPPING` 一行。
   - **还剩 12 条卡希尔的音换不掉**（全游戏只有 ch01 有这些文件，名单在 `anim.UNMATCHED`）：
     蹲攻叠加打击层 ×3、`MutuStand-P03`、`MutuJump-P01`、`Martial04`、3 号武器冲刺的三段风声，
     外加三条**永远不会响**的（4/5 号武器的）。都是打击 / 风声层，不是嗓音。
     用户要是还能听出来，只能自己造音频或者拿别的音顶（会是「别的角色的打击音」）。
   - `Jab00` / `Dash01~05` / `Dash-B04` 的攻击特效仍挂在熊骨 `Bone_Wp00_11` 上（原版没给爱琳画 CH03 版）。
     要改只能自己造 `.efx`，或者借别的角色的 —— 目前**不做**。
   - `Run-B00`（空手后退跑）在 ch01 / ch03 里**都不存在**，引擎自己回退到别的动作；没人报过问题，先不碰。
   - `Jump` / `Crouch` / `Damage` 不分武器，仍是卡希尔那套（D9 认下的代价）。

---

## 当前卡点

无。**服务端和客户端都在跑**（客户端 PID 见 `tools/screenshot.py <pid>`，本轮抓过一张）。
★ 本轮改的是资源卷 ⇒ **客户端要重启才生效**；服务端不用（`update-gamedata` 这次没改出 server 侧文件差异）。
★ 测试账号 `testuser1` 现在是 **Lv.4**，4 级门已经过了 ⇒ 用 `start.bat` 起就行，
不再需要 `BSHOOK_IRENE_LEVEL_FREE=1`（D2）。
⚠ 上一轮遗留：`tools/build-ver.config` 从 `0.3.3` 变成了 `0.3.5`，来源不明，没动它，等用户确认是不是自己改的。

---

## ⏳ 待用户验证

**已实机确认正常的**：第 4 个角色出现且名字是爱琳、房间面板有她、能进图能打、
三把武器的弹道 / 爆炸 / 回血特效、开枪有声（2026-09-15）；三把武器的模型和持枪动作、
冲刺攻击的动作和特效（V11）；**锤子没了、跑步摆臂、格斗 / 表情特效**（V10 / V12 / V13 ✅ 2026-09-16 用户）。

**待验**（重启客户端后）：

| # | 怎么做 | 期望看到 | 状态 |
|---|---|---|---|
| V14 | 切 3 号武器（에이리얼 슈터）发射几次图腾 | 发射 ✅ 已验收（水声）；**换弹**改成一声低柔的「灌进去」（`Water-Load`，0.82 s），不再是卡希尔的金属搭扣 | ⏳ |
| V15 | 挨几下打、格斗打一套 P/K 连招、跑一跑、蹲着走 | 受击闷哼 / 吆喝 / 脚步 / 蹲行 / 拳脚命中全换成 **ch100 艾丽亚丝**的声线（比卡希尔低一档，更接近爱琳自己选人时那把嗓子）| ⏳ |

**出问题时一次给全这 4 样**：① 卡在哪一条；② `game_patched/BigShot.rpt` 的**最后一段**；
③ `logs/bshook_*.log` 的**最后 50 行**；④ 崩溃地址（我拿 `tools/re_bs.py dis <地址>` 定位）。

---

## 给接手会话的七句话

1. **图里那个角色叫爱琳，`ChrIndex=3`，原版埋好没开，只差 `Models/Characters/ch03/`。**
   别再重新调研，结论和证据全在 `FINDINGS.md` §1。
2. **M2 必须排在 M4 之前。** 不补 `BigChrIcons_CN.smf`（24→28 帧）就打开闸，
   **一进大厅就崩** `C0000005 @0x430857`（§3）。守卫在 `mkchar.py --audit` 第 ⑦ 条。
3. **六道闸，不是五道。** 最容易漏的是 `0x44caaf`（大厅面板）——
   大厅 / 房间 6 格 / 战斗换人条是**三条独立代码路径**（§2）。
4. **`bot.BOT_CHARACTER_PANEL_IDS` / `BOT_DEFAULT_CHARACTERS` 已经钉成字面量 `(0,1,2)`**，
   故意不跟 `BASE_CHARACTER_IDS`。别「顺手」改回派生写法（§8 / `test_bot` 有守卫）。
5. **`character_id_of_item(4400001)` 必须继续返回 `None`** —— 爱琳不是角色卡，
   给她补一张卡会把 `0x55853c` 那处补丁架空（`test_gameserver` 有守卫）。
6. **爱琳的 86 个 `.mtn` 不再是 ch01 的逐字节克隆**（anim.py 改写过：7 个动作移植 + 全部打了枪口点补丁）。
   改动作走 `anim.py`，别手拷别的角色的 `.mtn`（骨架树不同，查不到骨就崩，§20）。
7. ★ **「克隆自卡希尔」≠「卡希尔有的她都该有」**：`J0000` 锤子（§21）和 `W0000` 泰迪熊（§22）
   都是卡希尔的**个人特例**，别的 15 个角色都没有。再看到「爱琳身上有件别人没有的东西」，
   先去 16 个角色目录里数一遍谁有。**动作也一样** —— 卡希尔的 `Dash00` / `Run-F00` 都是围着那只熊编的（§23）。
8. ★ **「哪个动作在什么时候放」看 `WAnimIdx`**：`Stand/Attack/Reload/Run-F/Run-B/Dash` 后面那两位
   就是当前武器的 `WAnimIdx`（§19）。`00` 一档属于 `[ch03-dash]`（空手 / 格斗模式），
   `01/02/03` 是三把枪。用户说「某个动作不对」，先问清是空手还是拿着哪把。
