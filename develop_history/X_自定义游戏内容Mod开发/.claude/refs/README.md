# refs —— 参考素材

放**用户提供的**参考图（原画、截图、对照图）。子代理读不到对话里的图片，
凡是要给子代理看的东西都得先落盘到这里。

| 文件 | 给谁用 | 状态 |
|---|---|---|
| `irene-concept.png` | X1 的 M7（Fable 5.1 子代理改爱琳的贴图/网格） | ⏳ **等用户另存** |

★ 爱琳还有一份**仓库里自带的权威参考**，不用另存：
`game_patched/Pack_develop/Images/NewUI2/BigChrIcons.png` 的**帧 26 / 27**
（矩形 `(315,617,470,692)` 和 `(1,694,156,769)`）—— 官方头像，银白长发 + 尖耳 +
浅灰绿眼 + 身后品红花瓣。配色以它为准。
